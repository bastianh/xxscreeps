---
"xxscreeps": patch
---

decorations.season serves default decorations in every room; player placements win; the map is unaffected. Ownerless `object` decorations are served too, so a season set can dress every controller on the server.
